# Luma Remote A3.40 产品电影视觉验收

日期：2026-09-04

## 参考输入

- 首屏构图：`/var/folders/6r/zc4nb2t97l5861m0yg32p0yr0000gn/T/codex-clipboard-ca4433e7-e31e-4601-baf4-7c55bd3647ee.png`
- All-in-One 空间爆炸图：`/var/folders/6r/zc4nb2t97l5861m0yg32p0yr0000gn/T/codex-clipboard-250b1c4c-18eb-47a9-9547-6e62f8dbd0a7.png`
- 暗场预告：`codex-clipboard-b912a5f0-c3b7-415c-ac0c-36e49a57452b.png`、`codex-clipboard-26ba157c-59fa-43a2-bc4c-fabb0d801136.png`
- 动效语言：[Anime.js](https://animejs.com/)

## 动态参考分析

Anime.js 首页不是静帧轮播，而是在一个持续存在的三维舞台上完成整段叙事：同一物体沿单一方向转动，外层结构先拉开，内部零件继续在原空间关系中展开；背景从深灰过渡到浅色，实体材质切换为完整线稿；指示线跟随零件移动，随后零件在同一坐标系中重新组合。页面通过持续的物体记忆、相机与结构动作错峰、背景/材质渐变和稳定进度标记避免硬切。

本页采用同样的连续原则，但保留 Luma Remote 自己的产品信息结构：24 个章节共享一个 Three.js 场景、同一组 231 个对象和一条 Anime.js 滚动时间线；相机始终顺时针，先完成相机过渡，再执行结构变化；从 231 对象总爆炸图进入 ESP32、显示、CPU、通信、IMU、存储、接口、电源和输入板介绍时，不重新切换模型。

## 同屏视觉对照

- 首屏对照用户提供的产品正面特写：当前画面同样把六键、屏幕和旋钮放在第一视觉层，标题位于左上安全区；键帽改为深灰哑光，3800K 轮廓光仅勾边，屏幕保留轻微玻璃反光。
- 爆炸图对照用户提供的 All-in-One 参考：当前版本保留完整外围结构作为空间记忆，Compute 组件在板相对坐标中二次展开，所有后续特写仍能看出零件来自同一块 ESP32-S3-LCD-2。
- 线稿对照 Anime.js 动态段落：实体、半透明 X-Ray、完整线稿、浅色技术场与深色终幕连续过渡；指示线从标签绘制到真实三维锚点，并随零件移动。

## 本轮发现并修复

- P1：ESP32 CPU/IMU/USB/MicroSD 特写的元器件展开过大，部分零件越出 1270 × 884 视口。已收紧板内径向与法向展开距离，并后退 15–19 镜头；六个计算特写均完整入镜。
- P1：显示堆栈过近，显示层和正文互相压住。已扩大镜头距离并把玻璃、背光和主 PCB 移入右侧安全区。
- P1：3D 打印镜头到章节末尾才完成摊开，稳定阅读窗口太短。已把平铺完成点提前到 60%，11 个可打印部件可同时看清。
- P1：页面残留“逐层开启”“完整回装”“Digital Twin”等制作说明。已全部换成装配边界、系统协同与产品能力文案；移除相机扩展的过度承诺。
- P2：打印、回装和终幕标题与主体靠得过近。已采用更窄的右侧文字安全区，并进一步把主体留在左侧。
- P2：首屏键帽过亮、材质偏塑料。已降低键帽亮度和发光强度；外壳、键帽与 PCB 均以高粗糙度、近零金属度材质渲染；屏幕仅保留受控反光。
- P3：生产构建仍提示单个 JavaScript chunk 超过 500 kB。当前运行时仅使用既有 Three.js 与 Anime.js，没有为该提示增加拆包架构或新依赖。

## 真实浏览器验收

- 浏览器：用户指定的 Codex 内置浏览器。
- 视口：1270 × 884。
- 冷加载：捕获到 `001% / 3 / 231`；模型完全隐藏，只有排版、单一进度条、数量和阶段提示。加载完成后状态为 `231 / 231`、`data-loading=false`、`data-error=null`，整机在第一帧一次显现，没有零件逐个蹦出。
- 连续性：逐段检查 07→10、11→22、22→24。相机方位单调顺时针；相机与结构动作错峰；背景、线稿、材质与透明度均连续插值。
- 防穿模：检查安全开壳和逆序回装中间态。键帽与旋钮先沿面法线抬升 8 mm，上壳随后开启，键轴和内部模块最后移动；回装顺序相反，未观察到键帽、键轴与上壳边缘交叉。
- 主体可见：内部层级、显示堆栈、LCD、CPU/Flash、Wi-Fi/MQTT/HA、QMI8658、MicroSD/USB-C、GPIO/电池检测、电源、输入板、11 个可打印件和最终整机均在稳定阅读区完整可辨，并与正文分区。
- 指示线：X-Ray、总爆炸、显示堆栈、CPU、通信、IMU、存储、GPIO、电源、输入板和打印件镜头均使用真实零件投影锚点；标签不越界、不遮挡正文。
- 内容准确性：页面使用 2.0 英寸、240 × 320、262K 色、ESP32-S3R8、8MB PSRAM、16MB Flash、Wi-Fi/MQTT/HA Discovery、QMI8658、MicroSD、USB-C、GPIO、电池检测和 11 个可打印部件；未把 BLE、Camera 或概念电源结构写成已经启用的量产功能。

## 自动验证

- 231 个 STL 与 manifest 一一对应；分组数量为 3 / 8 / 7 / 54 / 137 / 22。
- 6001 个时间采样确定性一致；镜头边界连续；相机方位不反转；结构动作不重叠；最终状态全部回零。
- Loading、失败态、线稿、真实三维指示线、产品文案禁词、3800K 屏幕状态与主要硬件事实均有静态契约检查。
- `npm run validate`、`npm run build`、`git diff --check` 为最终命令门禁。

final result: passed
