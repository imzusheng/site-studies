# Luma Remote A3.40 视觉验收

日期：2026-09-05

## 本轮目标

以用户提供的 1672 × 941 首屏参考为基准，重做 A3.40 首屏的产品构图、非金属哑光材质、约 3800K 轮廓光、屏幕玻璃层次和暗场显影；允许为网页展示细化模型，但不改变产品的六键、LCD 与旋钮识别结构。

参考图：`/var/folders/6r/zc4nb2t97l5861m0yg32p0yr0000gn/T/codex-clipboard-28e5f52c-42e9-433f-812a-e48ce30aa9ee.png`

## 同屏对照结论

最终同屏图：`/Users/lizusheng/.codex/visualizations/2026/09/04/01a06b8f-7ee4-70d1-bc26-dc803589908c/luma-hero-final-comparison.png`

- 构图：桌面画面保持正面、对称、满宽，产品上缘约落在画面 15%，屏幕完整可见，旋钮下缘有意裁切；标题、导航和进度在首屏初始状态隐藏，不遮挡产品。
- 明暗：整体仍是发布预告式暗场，由顶部宽轮廓和局部窄边勾出体积；没有大面积纯白键帽或高光剪切。
- 材质：外壳、键帽、旋钮均为 `metalness: 0` 的高粗糙度表面，并加入确定性的细颗粒微表面和面向曲率的柔和高光；视觉不再像光滑塑料或金属喷漆。
- 屏幕：LCD 由哑光框、低反射玻璃和暗色界面三层组成；仅保留窄幅反射，界面仍可读，不形成镜面。
- 模型细化：网页展示态放大并重整屏幕框、玻璃和旋钮比例；旋钮新增真实三维圆角中心标记；键帽位置沿面法线微调，首屏不再露出键轴或内部板件。

## Loading 与防穿模

- 加载期间整机保持隐藏，页面只展示统一的 231 部件进度、阶段与状态；231/231 完成并渲染首帧后再整体淡入，不会看到零件逐个出现。
- 装配态隐藏 Input、Compute、Power、Fasteners 与 EC11 内部参考体；进入技术镜头或开壳动作后才渐显。
- 键帽、旋钮和屏幕展示态使用独立的安全偏移；内部展开、键帽抬升和壳体开启仍按既有连续时间线错峰执行。
- 透明或线稿零件不再保留实体阴影，减少移动端和技术镜头的无效阴影绘制。

## 真实浏览器验收

使用本机 Microsoft Edge 151 的真实 Chromium/WebGL 页面，通过 CDP 等待 `231 / 231` 与首帧完成后截图；macOS 图形会话当时处于锁定状态，因此没有依赖桌面 UI 自动化。

- 桌面 1672 × 941：`luma-hero-final-v3.png`；与参考图同尺寸并已组合成最终对照图。
- 平板 1024 × 768：产品仍保持正面对称，屏幕、六键与旋钮可辨，允许边缘裁切维持压迫感。
- 手机 390 × 844：启用纵向相机补偿，六键、LCD、旋钮和外壳上缘同时留在画面中；不再只剩屏幕与旋钮。
- 交互抽查：滚动到 `#shot-03` 后，页面状态为 `controls`，滚动时间与电影时间均为 `2855.241`；说明滚动驱动与镜头状态同步。
- 运行状态：`data-error=null`、231 个对象加载完成、Loading 已隐藏；无 JavaScript exception、无浏览器 console error。

## 自动验证

- `npm run validate`：PASS；231 个 STL、11 个可打印件、24 个镜头、28100 时间单位、6001 个运动采样均符合连续性契约。
- `npm run build`：PASS；仅保留既有单包大于 500 kB 的非阻断警告。
- `git diff --check`：PASS。

final result: passed
